{
  "name": "@c-ferrier/lore-protocol",
  "version": "0.0.0",
  "description": "CLI tool for the Lore protocol — structured decision context in git commits",
  "type": "module",
  "bin": {
    "lore": "./bin/lore.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "bin/",
    "dist/",
    "README.md"
  ],
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "test": "vitest run",
    "lint": "eslint src/ tests/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@c-ferrier/atom-engine": "0.0.0",
    "chalk": "^5.6.2",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
