[validation]
max_message_lines = 50
subject_max_length = 72

[stale]
older_than = "6m"
drift_threshold = 20

[output]
default_format = "text"

[follow]
max_depth = 3

[cache]
query = true
identity = true
prune_threshold = 100

[cli]
update_check = true

[protocols]
